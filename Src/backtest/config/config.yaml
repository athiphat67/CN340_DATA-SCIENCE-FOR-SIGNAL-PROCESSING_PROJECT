# config.yaml
data:
  gold_csv: "data/premium_hsh/Mock_HSH_OHLC.csv"
  news_csv: "data/news_data/gold_macro_news_v1.csv"
  external_csv: "data/premium_hsh/Premium_Calculated_Feb_Apr.csv"

backtest:
  timeframe: "15m"
  days: 1  # ปรับเป็น 1 ปีตามเป้าหมาย
  react_max_iter: 1

llm:
  provider: "openrouter:gemini-3-1-flash-lite-preview"
  model: "" # ปล่อยว่างเพื่อใช้ default

paths:
  cache_dir: "output/backtest_cache_main"
  output_dir: "output/backtest_results_main"